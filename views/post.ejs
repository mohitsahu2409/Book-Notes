<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book.title %> - Book Notes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

  <div class="container mt-5">
    <div class="card shadow-sm">
      <% if (book.cover_url) { %>
        <img src="<%= book.cover_url %>" class="card-img-top img-fluid" alt="Cover Image" style="max-height: 300px; object-fit: contain;">
    <% } %>

      <div class="card-body">
        <h2 class="card-title"><%= book.title %></h2>

        <ul class="list-group list-group-flush my-3">
          <li class="list-group-item"><strong>Author:</strong> <%= book.author %></li>
          <li class="list-group-item"><strong>Date Read:</strong> <%= new Date(book.date_read).toISOString().split("T")[0] %></li>
          <li class="list-group-item"><strong>Rating:</strong> <%= book.rating %> / 5</li>
        </ul>

        <% if (book.review) { %>
          <h5 class="mt-4">Review</h5>
          <p class="card-text"><%= book.review %></p>
        <% } else { %>
          <p class="text-muted fst-italic">No review provided.</p>
        <% } %>

        <a href="/" class="btn btn-outline-primary mt-3">← Back to Home</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
